import styled from "styled-components";

interface LoaderProps {
    size: number
}

const LoaderWrapper = styled.div`
  
`
export const Loader = () => {
    return (
        <LoaderWrapper>
            
        </LoaderWrapper>
    );
};